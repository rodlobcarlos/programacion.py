#Ejecuta y entiende  qué valor se devolverá con cada una de estas expresiones:
#Los dos puntos marcan un intervalo. 
lista[:] # Se devlverá el valor entero.
lista[0:len(lista)] # Se contará todo el valor desde el elemento 0.
lista[-len(lista):] # Se contará todo el valor a partir del elemento -1, entonces sería a partir del último elemento hacia atras.
lista[:len(lista)] # Se contará todos los elementos.
lista[len(lista)-1] # Se contará todos los elementos menos el último. 
lista[-1] # Se contará el último elemento de la lista.